services:
  web:
    build: .
    image: kimbeca:latest
    container_name: kimbeca
    restart: unless-stopped
    ports:
      # Bound to localhost only — nginx (or an SSH tunnel for testing)
      # is the only thing that should reach this port on the server.
      - "127.0.0.1:3000:3000"
